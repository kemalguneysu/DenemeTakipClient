import { Delete3Directive } from './delete3.directive';

describe('Delete3Directive', () => {
  it('should create an instance', () => {
    const directive = new Delete3Directive();
    expect(directive).toBeTruthy();
  });
});
